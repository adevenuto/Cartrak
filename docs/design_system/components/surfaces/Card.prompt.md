One-line: The base white rounded surface used everywhere — wrap content that should sit on a card.

```jsx
<Card pad="lg" elevation="md">…</Card>
```

`radius` defaults to `lg` (22px). `elevation` maps to the shadow scale; use `flat` for cards on white. Compose StatCard / VehicleCard on top of this.
