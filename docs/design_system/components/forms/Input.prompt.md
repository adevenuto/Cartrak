One-line: A rounded text/search field on a white chip — the "Search Services" bar and any form field.

```jsx
<Input placeholder="Search Services" leftIcon={<SearchIcon/>} />
<Input placeholder="Email" rounded="md" />
```

Pill-shaped by default (`rounded="md"` for softer form fields). Border turns crimson on focus. Pass `leftIcon` for the search pattern.
