One-line: The signature "Vehicle Score" dial — a segmented half-circle that fills with the crimson gradient.

```jsx
<ScoreGauge value={85} max={100} label="Vehicle Score" sublabel="Target Score 100" />
```

Segments light up left-to-right, dark crimson deepening toward the filled end; unfilled segments are neutral gray. The center holds a glyph (defaults to a bolt), the number sits below.
