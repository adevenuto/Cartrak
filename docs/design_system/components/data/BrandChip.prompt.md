One-line: A brand tile for the "Brands" carousel — a white logo chip with the brand name and a "+N" model count.

```jsx
<BrandChip name="Mercedes" count={32} logo={<img src="…" alt="" width={44}/>} active />
```

Selected state gets a crimson ring + glow. Lay several in a horizontally scrolling row.
