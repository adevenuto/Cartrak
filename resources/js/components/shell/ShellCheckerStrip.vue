<template>
    <!--
      The checkered-flag motif, reused as a 10px texture strip between the navy
      header and the content. Design doc §1: decoration, used exactly once per
      page — which is why it lives in AppShell rather than being something a
      screen remembers to add.
    -->
    <div class="ii-checker shrink-0" aria-hidden="true" />
</template>
